apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusterfleets.runeward.dev
spec:
  group: runeward.dev
  scope: Cluster
  names:
    kind: ClusterFleet
    listKind: ClusterFleetList
    plural: clusterfleets
    singular: clusterfleet
    shortNames:
      - cflt
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: |
            ClusterFleet is a cluster-scoped declarative request for N governed
            runeward sandboxes that share a single atomic task board, shared
            across an organization. It behaves like Fleet but is not namespaced;
            the runeward controller provisions the backing resources in its own
            (controller) namespace using the named profile's [fleet] block.
          properties:
            spec:
              type: object
              required:
                - profile
              properties:
                profile:
                  type: string
                  description: Name of the runeward profile (with a [fleet] block).
            status:
              type: object
              properties:
                phase:
                  type: string
                  description: One of Pending, Running, Failed, Terminating.
                fleetId:
                  type: string
                sandboxes:
                  type: array
                  items:
                    type: string
                tasksTotal:
                  type: integer
                tasksPending:
                  type: integer
                tasksClaimed:
                  type: integer
                tasksDone:
                  type: integer
                tasksFailed:
                  type: integer
                message:
                  type: string
                observedGeneration:
                  type: integer
                  format: int64
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Profile
          type: string
          jsonPath: .spec.profile
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Fleet
          type: string
          jsonPath: .status.fleetId
        - name: Done
          type: integer
          jsonPath: .status.tasksDone
        - name: Total
          type: integer
          jsonPath: .status.tasksTotal
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
