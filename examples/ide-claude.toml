# Browser IDE spin-off with Claude Code CLI in-cell.
#
# Claude Desktop has no self-hosted browser GUI — Open IDE is code-server;
# run `claude` in the IDE terminal (or dashboard Terminal tab).
#
#   docker build -f deploy/Dockerfile.ide-agents -t runeward-ide-agents:latest .
#   printf '%s' "$ANTHROPIC_API_KEY" > ~/.runeward-anthropic.key
#   RUNEWARD_ENABLE_EXPERIMENTAL_IDE=1 ./bin/runeward --config-dir examples serve
#   Create charter "ide-claude" → Open IDE → terminal: claude

[host]
type    = "container"
image   = "runeward-ide-agents:latest"
user    = "coder"
workdir = "/workspace"

[ide]
enabled = true
port    = 8080
agents  = ["claude"]

[network]
default = "deny"

[[network.rule]]
verdict  = "allow"
hostname = "api.anthropic.com"

[[network.rule]]
verdict  = "allow"
hostname = "*.anthropic.com"

[[env]]
name = "ANTHROPIC_API_KEY"
file = "~/.runeward-anthropic.key"

[[env]]
name  = "NODE_USE_ENV_PROXY"
value = "1"

[[policy]]
tool    = "shell"
match   = "rm -rf*"
verdict = "deny"
reason  = "destructive recursive delete blocked by policy"

[rationing]
wall_clock      = "2h"
max_execs       = 500
egress_requests = 2000

[chronicle]
redact = true
